name: validate
on:
  push:
  pull_request:

jobs:
  basic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Sanity check scripts
        run: |
          python -m py_compile scripts/ingest_chatgpt_export.py scripts/aplicar_candidatos.py
